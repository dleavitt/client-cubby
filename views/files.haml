%form(action="/logout" method="post")
  %input(type="submit" value="Logout")

%form{:action => "/u/#{user.name}", :method => "post", :enctype => "multipart/form-data"}
  %input(type="file" name="file")
  %input(type="submit")